if ($window.width() > 849) {
    $dscrollTop = $doc.scrollTop();



// when you start scrolling, make the header sticky

// when the header is sticky, begin subtracting the dscrollTop from the header top while also shrinking the logo width

// once the logo has reached its final width, animate the nav margin down to zero
